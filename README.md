# Dr Pippa Robertson — website

Static site. Deploys to Vercel with no build step.

- `index.html` — home
- `conditions/`, `procedures/` — sub-pages
- `assets/` — images, favicon
- `vercel.json` — clean URLs (`/conditions/fibroids`)
